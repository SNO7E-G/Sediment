# Dependencies
/vendor/
# Library, not app: let each PHP version resolve its own constraints fresh.
composer.lock

# Internal design spec — not published to the repo.
sediment-project-spec.md

# Internal hardening/expansion plan — local only, never published.
sediment-hardening-plan.md

# Build artifacts
/build/
*.phar

# PHPUnit
/.phpunit.cache/
.phpunit.result.cache

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Local overrides
phpunit.xml
sediment.local.yml
