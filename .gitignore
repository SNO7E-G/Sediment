# Dependencies
/vendor/
# Library, not app: let each PHP version resolve its own constraints fresh.
composer.lock

# Internal design spec — not published to the repo.
sediment-project-spec.md

# Build artifacts
/build/
*.phar

# PHPUnit
/.phpunit.cache/
.phpunit.result.cache

# Local assistant / code-index tooling — never pushed
.codegraph/
.serena/

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Local overrides
phpunit.xml
sediment.local.yml
