# Dependencies
/vendor/
# Library, not app: let each PHP version resolve its own constraints (8.1 -> symfony 6.4, 8.2+ -> 7.x)
composer.lock

# Build artifacts
/build/
*.phar

# PHPUnit
/.phpunit.cache/
.phpunit.result.cache

# Local assistant / code-index tooling — never pushed
.codegraph/
.serena/

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Local overrides
phpunit.xml
sediment.local.yml
